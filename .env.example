DATABASE_URL="postgresql://qrconso:qrconso@localhost:5433/qrconso?schema=public"

# Signs admin session cookies and QR code payloads. Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
APP_SECRET="dev-secret-change-me-0000000000000000000000000000"

# Printed in clear under every QR code so consumers can check it before scanning (note de cadrage §9)
NEXT_PUBLIC_QR_DOMAIN="qr.consommateurs.ma"

# Base URL encoded into generated QR codes
QR_BASE_URL="http://localhost:3000"
